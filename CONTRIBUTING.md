# ブランチ運用

一人でも安全に開発と本番反映を続けられるよう、レビュー待ちではなく PR、必須 CI、セルフレビューを品質ゲートにします。

## 基本フロー

- 作業ブランチは最新の `develop` から `<担当者>/<目的>` の形式で作成します。
- `main` は誤削除を防ぐため GitHub のデフォルトブランチに設定します。
- GitHub が新規 PR の宛先に `main` を初期選択するため、通常変更では宛先を必ず `develop` に変更します。
- 通常変更は作業ブランチから `develop` へ PR を作成します。
- PR では `Public verify` と `Admin verify` の成功を必須とします。
- 通常の作業 PR は Squash merge し、マージ後に作業ブランチを自動削除します。
- 本番反映は `develop` から `main` への PR だけで行います。
- 本番反映後は `main` から `develop` への同期 PR を作成します。
- `develop` と `main` への直接 push、force push、ブランチ削除は禁止します。

## 本番反映の確認

`develop` から `main` への PR に他者承認は必須としません。マージ前に次を確認します。

- `Public verify` と `Admin verify` が最新コミットで成功している
- 最新差分を自分で読み直している
- 未検証事項、影響範囲、必要なロールバック方法を PR に記録している
- マージ後に `main` から `develop` への同期 PR を作成する

認証、権限、課金、DB 変更、大規模なデータ更新などの高リスク変更では、可能な場合のみ外部レビューを依頼します。レビュー待ちを通常運用の必須条件にはしません。

## Hotfix

緊急修正は最新の `main` から `<担当者>/hotfix-<目的>` を作成し、`main` への PR で反映します。反映後は直ちに `main` から `develop` への同期 PR を作成します。

## 自動化と制約

- `Branch flow` が PR の向き、作業ブランチ名、Hotfix の起点を確認します。
- `CI` が `Public verify` と `Admin verify` を実行します。
- マージ済みの作業ブランチはワークフローで削除します。`develop` と `main` は削除対象外です。
- `main` はデフォルトブランチのため GitHub 上で削除できません。
- 現在の GitHub プランではブランチ保護を設定できないため、`develop` の削除や、直接 push、force push、CI を通さないマージを GitHub 側で完全には阻止できません。その他のルール違反はワークフローと運用で防ぎます。
