# Hugmeid「データが吸い上げられない」障害 — 調査・修正まとめ (2026-09-10)

## 結論 (根本原因)

アプリ本体のコードではなく、**DB 接続先の設定ミス** と **スキーマ適用漏れ** が原因でした。

| # | 原因 | 影響 |
|---|------|------|
| 1 | **接続先DBの誤り** — 環境変数が空の `hugmeid_production_v2` を指していたが、実データ (記事30件・求人・課外活動・ユーザー等) は別DB `hugmeid_production_v3` に存在した | 公開アプリ・管理画面ともに「データが吸い上げられない」(空に見える) 状態 |
| 2 | **v2 への最新マイグレーション未適用** — v2 は 2026-08-25 時点のスキーマで停止しており、9/2 以降の `click_count`・サムネイル・共有時間割等のカラム/テーブルが無かった | 管理画面ダッシュボードが `column "click_count" does not exist` で「データベースに接続できません」エラー |
| ※ | v3 のテーブルは `postgres` 所有で、設定されていた `hugmeid_schema_owner` には SELECT 権限すら無かった | 単に `PGDATABASE` を v3 に書き換えるだけでは動かないため、**データを v2 へ移行**する方針を採用 |

---

## 実施した修正

### A. データベース修復 (前回セッションで実施済み・FBのとおり)

1. **v2 へ最新マイグレーション 6 件を適用** (click_count エラー解消)
   - `20260902000000_content_activity_thumbnails`
   - `20260903000000_click_tracking`
   - `20260903000001_shared_timetable`
   - `20260906000020_timetable_university_grade_scope`
   - `20260906000030_timetable_user_overrides`
   - `20260908000000_job_thumbnails`
2. **v3 の実データを v2 へ移行** (合計 423 行 / 全 22 テーブルで v2 = v3 を検証済み)
3. **runtime ロールへの権限付与** (`hugmeid_public_runtime` / `hugmeid_admin_runtime`)

> 再適用・検証用に冪等な SQL を同梱: `cloudsql/ops/20260910_repair_v2_from_v3.sql`

### B. コード / 設定の修正 (今回の差分)

| ファイル | 変更 | 理由 |
|---|---|---|
| `.env.production` | `SESSION_SECRET` を `change-me-...` (未達の危険な既定値) → 64 hex の本番値に差し替え | 32 文字未満だと公開側の起動時チェック `assertPublicRuntimeConfig` で 503 / 起動失敗になる |
| `.env.production` | `REVALIDATE_ADMIN_SECRET` を `change-me-shared-secret` → 64 hex に差し替え | 公開↔管理のキャッシュ無効化連携のシークレット一致が必須 |
| `admin/.env.production` | `REVALIDATE_ADMIN_SECRET` を同じ 64 hex に差し替え | 上記と値が一致しないと `POST /api/admin/revalidate` が 401 で失敗し、公開側キャッシュが更新されない |
| `.env.production` | `NEXT_PUBLIC_SITE_URL` が admin の URL になっていた件に TODO コメントを追記 | 公開アプリの URL 確定後に差し替える必要があるため (誤設定のまま放置しないよう明示) |
| `admin/app/api/health/route.ts` | **新規追加** — 公開側と同等のヘルスチェック (`/api/health`) | admin 側に health が無く、Cloud Run / 監視が DB 接続不良を検知できなかった |
| `cloudsql/ops/20260910_repair_v2_from_v3.sql` | **新規追加** — 修復の再適用・検証用の冪等 SQL | 再発時の復旧を一発で行えるようにするため |

---

## 設定手順 (Cloud Run への反映)

### 0. 前提
- 対象プロジェクト: `hugmeid-test-507423` / リージョン: `northamerica-northeast2`
- サービス: 管理画面 `hugmeid-admin`、公開アプリ (別サービス)
- 認証: `gcloud auth login` 済みで当該プロジェクトの編集権限があること

### 1. (DB が未修復の場合のみ) マイグレーション + 修復 SQL
```bash
cd kakunin-main
# 未適用マイグレーションを適用 (db:migrate が安全に順次適用)
npm run db:migrate
# 修復・権限・帳簿合わせ (冪等なので再実行可)
psql "host=34.130.33.231 port=5432 dbname=hugmeid_production_v2 user=hugmeid_schema_owner" \
  -f cloudsql/ops/20260910_repair_v2_from_v3.sql
```

### 2. 環境変数を Cloud Run に反映
`.env.*` は `.gcloudignore` でデプロイに含まれないため、**Cloud Run のサービス設定に直接登録**します。
```bash
# 管理画面
gcloud run services update hugmeid-admin \
  --region northamerica-northeast2 \
  --env-file admin/.env.production

# 公開アプリ (サービス名は実際のものに置き換える)
gcloud run services update hugmeid-web \
  --region northamerica-northeast2 \
  --env-file .env.production
```
> **推奨**: `PGPASSWORD` / `SESSION_SECRET` / `REVALIDATE_ADMIN_SECRET` は
> `--env-file` ではなく Secret Manager 参照で登録するのが安全です。
> (例: `--set-secrets=PGPASSWORD=hugmeid-pgpassword:latest,...`)

### 3. デプロイ
```bash
# 管理画面
cd admin && npm ci && npm run build && cd ..
gcloud run deploy hugmeid-admin --source admin --region northamerica-northeast2

# 公開アプリ
npm ci && npm run build
gcloud run deploy hugmeid-web --source . --region northamerica-northeast2
```

### 4. 動作確認
```bash
# ヘルスチェック (200 / {"ok":true} になれば DB 接続 OK)
curl -i https://<admin-url>/api/health
curl -i https://<public-url>/api/health

# 管理画面ダッシュボードに件数が出ること
# 全記事 30 / 求人 1 / 課外活動 4 / 画像 19 / 有効なメンバー 4
```

---

## 残っている注意点 (今後の推奨対応)

- **管理画面の認証**: 依頼③により認証が完全廃止されており、URL を知る全員が owner 権限で操作可能です。**一般公開前に必ず認証を復活**させてください (`admin/lib/auth/open-access.ts` の `OPEN_ACCESS_ALWAYS_ON` を `false`)。
- **画像アセット URL**: DB 内の画像 URL が指す配信ホストと `GCS_PUBLIC_ASSET_BASE_URL` の整合を確認してください。サムネイルが表示されない場合はここを見直し。
- **v3 DB**: データは v2 に移行済みですが、バックアップとして削除せず残すことを推奨します。
- **シークレット管理**: DB 接続情報・パスワードが `.env.production` に平文で含まれているため、Secret Manager への移行を推奨します。
- **`NEXT_PUBLIC_SITE_URL`**: 公開アプリの URL 確定後に差し替えてください。
