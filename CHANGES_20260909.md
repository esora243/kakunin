# 変更点まとめ (2026-09-09)

依頼①〜④ に対応した修正の一覧です。

---

## ① 認証情報・接続情報の更新

新規ファイル:

| ファイル | 内容 |
|---|---|
| `admin/.env.production` | 管理画面 (Cloud Run: hugmeid-admin) 用の本番環境変数。DB接続 (34.130.33.231 / hugmeid_production_v2 / hugmeid_schema_owner)、GCS バケット `hugmeid-test`、LIFF ID、インスタンス接続名 `hugmeid-test-507423:northamerica-northeast2:hugmeid-test` 等を記載 |
| `.env.production` | 公開アプリ側の本番環境変数。同じDB接続情報・LIFF 情報・サイトURLを記載 |

※ 既存コードは `PGHOST`/`PGDATABASE`/`PGUSER`/`PGPASSWORD`/`CLOUD_SQL_CONNECTION_NAME` を読む構造になっており、ユーザ指定の `DB_HOST` 等のエイリアスは読まれないため、両者を対応付けた値で記載しています。

---

## ② サムネイル/記事上部画像アップロードのバグ修正

**原因**: `admin/app/api/assets/upload/route.ts` が、ブラウザの `FormData` 送信時の
`Content-Type: multipart/form-data; boundary=...` を画像 MIME と判定できず
必ず 415 を返していたこと、および `target=jobs` (求人) が未対応で 400 になっていたこと。

修正ファイル:

- `admin/app/api/assets/upload/route.ts`
  - `multipart/form-data` のボディを受理し、実際の画像種別は既存の sharp デコード検証に委ねるように変更
  - アップロード target に `jobs` を追加 (記事・求人・課外活動の全てで共通利用可能に)

これにより、記事のサムネイル・ヒーロー(記事上部)画像、求人画像、課外活動画像の
アップロードが正常に動作します。

---

## ③ 管理画面の認証を完全に廃止 (URL を知っていれば誰でも利用可)

**v2 追記**: 初版では環境変数 `ADMIN_OPEN_ACCESS=true` が必要でしたが、
「認証がまだ残っている」との指摘を受け、**コード上で常時有効 (環境変数の設定は一切不要)** に変更しました。
アプリをデプロイすれば、その時点で Google ログイン画面は一切出ず、誰でもそのまま管理画面に入れます。

修正/新規ファイル:

- `admin/lib/auth/open-access.ts` **(新規)** — 常時オープンアクセス。組み込み owner 権限 ID (`open-access@hugmeid.local`) を発行。認証を復活させる場合はこのファイルの `OPEN_ACCESS_ALWAYS_ON` を `false` にする
- `admin/lib/auth/admin-session.ts` — セッション解決の先頭でオープンアクセスを判定 (Google OAuth・admin_users ルックアップをスキップ)
- `admin/lib/api-route.ts` — API の Origin チェックをスキップ (オープンアクセスを先に評価し、dev バイパス判定の例外で阻害されないよう評価順も修正)
- `admin/app/layout.tsx` — アクセス監査の DB 書き込みをスキップ (同様に評価順を修正)
- `admin/app/logout/route.ts` — 現在のオリジンへリダイレクト (`GOOGLE_OAUTH_REDIRECT_URI` 不要)
- `admin/lib/runtime-config.ts` — `GOOGLE_OAUTH_*` / `ADMIN_SESSION_SECRET` を必須から除外
- `admin/.env.example` / `admin/.env.production` — 認証不要の旨を追記 (設定自体は不要)
- `admin/tests/auth-session.test.ts` / `admin/tests/admin-api-route.test.ts` / `admin/tests/oauth-routes.test.ts` / `admin/tests/runtime-config.test.ts` — 認証必須を前提にした旧テストを、常時オープンアクセスの新仕様に合わせて更新 (全160件パス)

**注意**: URL を知る全員が全機能を owner 権限で操作できます。一般公開前には必ず認証を復活させてください。

---

## ④ 時間割の管理画面からの追加 + CSV 一括取り込み

管理画面の「時間割」ページ (`/school/timetable`) には既に1件ずつの追加機能があり、
リンク切れだった **CSV 一括登録ページを新設** しました。

新規ファイル:

- `admin/app/school/timetable/import/page.tsx` — CSV取り込みページ (`/school/timetable/import`)
- `admin/components/school/TimetableCsvImporter.tsx` — ファイル選択・取り込み実行・結果表示・サンプルCSVダウンロードのUI
- `admin/app/api/school/timetable/import/route.ts` — 取り込み API (`POST /api/school/timetable/import`)
- `admin/lib/timetable-csv.ts` — CSV パーサ + バリデーション + 一括 INSERT ロジック

CSV フォーマット (1行目はヘッダー、UTF-8 / BOM 可 / 最大 2MB・1000行):

```csv
university_name,academic_year,term_number,department_label,class_title,day_of_week,period,room,instructor,note,source_url,sort_order
東京大学,2026,1,医学部,解剖学Ⅰ,月,1,101講義室,山田太郎,,https://example.edu/timetable,0
```

- ヘッダーは英語 (university_id 等) / 日本語 (大学名, 年度, 学期, 学科, 科目名, 曜日, 時限, 教室, 担当教員, 備考, 引用元URL, 表示順) 両対応
- `university_id` の代わりに `university_name` (大学名) で指定可。未登録の大学は自動登録
- 同一の大学・年度・学期・学科・科目・曜限の既存行はスキップ (重複登録しない)
- 行単位のエラーは取り込みを中断せず結果に集約して表示

あわせて以下の既存不整合も修正:

- `admin/components/school/SchoolTimetableManager.tsx` — 存在しない `Table`/`TableColumn` エクスポートを使っておりビルド不能だったため、実在する `TableShell`/`THead`/`Th`/`Tr`/`Td` コンポーネントで書き直し
- `admin/app/api/admin/timetable/route.ts` — 存在しない import パス (`@/auth/...`) と `zod` (未導入) への依存でビルド不能だったため、`adminApiRoute` + 手書きバリデーションで書き直し

---

## マイグレーション

`cloudsql/migrations/20260906000020_timetable_university_grade_scope.sql`
(syllabus_pages への grade 列追加) は添付ファイルと同一内容であることを確認済み。
未適用の場合は以下で適用:

```bash
npm run db:migrate
```

---

## デプロイ手順 (Cloud Run)

```bash
# 1. マイグレーション適用 (grade 列)
npm run db:migrate

# 2. admin アプリのビルド
cd admin && npm ci && npm run build

# 3. 環境変数を反映してデプロイ
gcloud run services update hugmeid-admin \
  --region northamerica-northeast2 \
  --env-file admin/.env.production
```

## 検証済み

- `admin/` の TypeScript 型チェック: エラーなし
- `admin/` のテストスイート: 全件パス
